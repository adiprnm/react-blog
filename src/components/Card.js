import styled from "styled-components";

const Card = styled.div`
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  flex-wrap: wrap;
  width: 100%;
  background-color: #ffffff;
  border-radius: 0.5rem;
  padding: 1.5rem 2rem;
  box-shadow: 0.25rem 0.25rem 0.25rem #bdbdbd;
  margin: 0;
`;

export default Card;
